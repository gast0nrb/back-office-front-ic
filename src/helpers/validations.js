const validarPrecios = () => {
    /* Chequear que al postear un precio,
     este debe ir con el monto si es que tiene un descuento.
     y debe ser mayor a 0 el monto.

     Si existe un monto en post, debe al menos existir cantidad_min.
    -
    Chequear que al hacer un update del precio
    el precio no puede ir con descuento, sino  tiene monto.
    Además, el precio debe ser mayor a 0 para realizar descuento. 
    */
}